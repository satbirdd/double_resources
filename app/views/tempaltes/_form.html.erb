<%= form_for(@tempalte) do |f| %>
  <% if @tempalte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tempalte.errors.count, "error") %> prohibited this tempalte from being saved:</h2>

      <ul>
      <% @tempalte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :filename %><br>
    <%= f.text_field :filename %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
